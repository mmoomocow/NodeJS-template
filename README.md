# NodeJS-template

**A template for nodeJS projects. Has basic workflows (lint with eslint and test) and issue templates**

_You need to install eslint yourself_
```
npm install eslint --save-dev
```
